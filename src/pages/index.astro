---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import CinematicHero from '../components/CinematicHero.astro';
import ProfileCard from '../components/ProfileCard.astro';
import PageLoader from '../components/PageLoader.astro';
import ScrollProgress from '../components/ScrollProgress.astro';
import BackToTop from '../components/BackToTop.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			/* 隐藏个人信息卡片，使用新的布局 */
			profile-card {
				display: none;
			}

			.main-content {
				background: #0a0a0a;
				min-height: 100vh;
			}

			/* 内容区域 */
			.content-section {
				max-width: 1400px;
				margin: 0 auto;
				padding: 6em 3em;
			}

			.section-header {
				text-align: center;
				margin-bottom: 4em;
			}

			.section-label {
				display: inline-block;
				padding: 0.5em 1.5em;
				background: rgba(255, 255, 255, 0.05);
				border: 1px solid rgba(255, 255, 255, 0.1);
				color: rgba(255, 255, 255, 0.6);
				font-size: 0.85em;
				text-transform: uppercase;
				letter-spacing: 0.2em;
				margin-bottom: 1em;
				backdrop-filter: blur(10px);
			}

			.section-title {
				font-size: clamp(2.5em, 6vw, 4em);
				font-weight: 800;
				color: #ffffff;
				margin: 0;
				line-height: 1.1;
				letter-spacing: -0.02em;
			}

			/* 特色网格 */
			.features-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: 2em;
				margin-bottom: 6em;
			}

			.feature-card {
				background: rgba(255, 255, 255, 0.02);
				border: 1px solid rgba(255, 255, 255, 0.08);
				padding: 2.5em;
			position: relative;
			overflow: hidden;
			transition: all 0.4s ease;
		}

		.feature-card::before {
			content: '';
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background: linear-gradient(135deg, rgba(99, 102, 241, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
			opacity: 0;
			transition: opacity 0.4s ease;
		}

		.feature-card:hover::before {
			opacity: 1;
		}

		.feature-card:hover {
			transform: translateY(-10px);
			border-color: rgba(99, 102, 241, 0.3);
			box-shadow: 0 30px 60px rgba(0, 0, 0, 0.5);
		}

		.feature-icon {
			width: 60px;
			height: 60px;
			background: linear-gradient(135deg, rgba(99, 102, 241, 0.2) 0%, rgba(139, 92, 246, 0.2) 100%);
			border: 1px solid rgba(255, 255, 255, 0.1);
			border-radius: 16px;
			display: flex;
			align-items: center;
			justify-content: center;
			font-size: 2em;
			margin-bottom: 1.5em;
			position: relative;
			z-index: 1;
		}

		.feature-title {
			font-size: 1.5em;
			font-weight: 700;
			color: #ffffff;
			margin: 0 0 1em 0;
			position: relative;
			z-index: 1;
		}

		.feature-description {
			color: rgba(255, 255, 255, 0.6);
			line-height: 1.7;
			margin: 0;
			position: relative;
			z-index: 1;
		}

		/* 博客展示区 */
		.blog-showcase {
			background: linear-gradient(180deg, transparent 0%, rgba(99, 102, 241, 0.05) 50%, transparent 100%);
			padding: 4em;
			border: 1px solid rgba(255, 255, 255, 0.05);
			margin-bottom: 6em;
			position: relative;
			overflow: hidden;
		}

		.blog-showcase::before {
			content: '';
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background: radial-gradient(ellipse at top, rgba(99, 102, 241, 0.1) 0%, transparent 50%);
			pointer-events: none;
		}

		.showcase-content {
			position: relative;
			z-index: 1;
			display: grid;
			grid-template-columns: 1fr 1fr;
			gap: 4em;
			align-items: center;
		}

		.showcase-text {
			padding: 2em;
		}

		.showcase-title {
			font-size: clamp(2em, 4vw, 3em);
			font-weight: 800;
			color: #ffffff;
			margin: 0 0 1em 0;
			line-height: 1.2;
		}

		.showcase-description {
			color: rgba(255, 255, 255, 0.7);
			font-size: 1.1em;
			line-height: 1.8;
			margin-bottom: 2em;
		}

		.showcase-stats {
			display: grid;
			grid-template-columns: repeat(2, 1fr);
			gap: 2em;
		}

		.stat-item {
			text-align: center;
		}

		.stat-number {
			font-size: 3em;
			font-weight: 800;
			background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
			-webkit-background-clip: text;
			-webkit-text-fill-color: transparent;
			background-clip: text;
		}

		.stat-label {
			color: rgba(255, 255, 255, 0.5);
			font-size: 0.9em;
			text-transform: uppercase;
			letter-spacing: 0.1em;
		}

		.showcase-visual {
			position: relative;
		}

		.blog-preview {
			background: rgba(255, 255, 255, 0.03);
			border: 1px solid rgba(255, 255, 255, 0.08);
			border-radius: 20px;
			padding: 2em;
			backdrop-filter: blur(10px);
		}

		.preview-card {
			background: rgba(255, 255, 255, 0.02);
			border: 1px solid rgba(255, 255, 255, 0.05);
			border-radius: 12px;
			padding: 1.5em;
			margin-bottom: 1em;
			transition: all 0.3s ease;
		}

		.preview-card:hover {
			background: rgba(255, 255, 255, 0.05);
			transform: translateX(10px);
		}

		.preview-card-title {
			color: #ffffff;
			font-size: 1.1em;
			font-weight: 600;
			margin: 0 0 0.5em 0;
		}

		.preview-card-meta {
			color: rgba(255, 255, 255, 0.4);
			font-size: 0.85em;
		}

		/* CTA 区域 */
		.cta-section {
			text-align: center;
			padding: 6em 3em;
			background: linear-gradient(180deg, transparent 0%, rgba(99, 102, 241, 0.1) 100%);
			border-top: 1px solid rgba(255, 255, 255, 0.05);
		}

		.cta-title {
			font-size: clamp(2em, 5vw, 3.5em);
			font-weight: 800;
			color: #ffffff;
			margin: 0 0 1em 0;
		}

		.cta-description {
			color: rgba(255, 255, 255, 0.6);
			font-size: 1.2em;
			max-width: 600px;
			margin: 0 auto 2em auto;
			line-height: 1.7;
		}

		.cta-button {
			display: inline-flex;
			align-items: center;
			gap: 0.75em;
			padding: 1.25em 3em;
			background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
			color: #ffffff;
			font-size: 1.1em;
			font-weight: 600;
			text-transform: uppercase;
			letter-spacing: 0.1em;
			text-decoration: none;
			border-radius: 50px;
			box-shadow: 0 10px 40px rgba(99, 102, 241, 0.4);
			transition: all 0.3s ease;
		}

		.cta-button:hover {
			transform: translateY(-3px);
			box-shadow: 0 20px 60px rgba(99, 102, 241, 0.6);
		}

		@media (max-width: 1024px) {
			.content-section {
				padding: 4em 2em;
			}

			.showcase-content {
				grid-template-columns: 1fr;
				gap: 3em;
			}

			.showcase-stats {
				grid-template-columns: repeat(4, 1fr);
			}
		}

		@media (max-width: 768px) {
			.features-grid {
				grid-template-columns: 1fr;
			}

			.blog-showcase {
				padding: 2em;
			}

			.showcase-stats {
				grid-template-columns: repeat(2, 1fr);
			}

			.cta-section {
				padding: 4em 2em;
			}

			.cta-button {
				width: 100%;
				justify-content: center;
			}
		}
		</style>
	</head>
	<body class="dark">
		<PageLoader />
		<ScrollProgress />
		<Header />
		<CinematicHero
			title="我的博客"
			subtitle="EXPLORE · DISCOVER · RECORD"
			backgroundText="BLOG"
		/>

		<div class="main-content">
			<section class="content-section">
				<div class="section-header">
					<span class="section-label">关于</span>
					<h2 class="section-title">探索技术与生活</h2>
				</div>

				<div class="features-grid">
					<div class="feature-card">
						<div class="feature-icon">⚡</div>
						<h3 class="feature-title">极速体验</h3>
						<p class="feature-description">基于 Astro 构建，毫秒级加载速度，提供流畅的浏览体验</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">🎨</div>
						<h3 class="feature-title">独特设计</h3>
						<p class="feature-description">新野兽派风格，深色主题，充满动效的现代化界面</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">📱</div>
						<h3 class="feature-title">完美适配</h3>
						<p class="feature-description">响应式设计，在任何设备上都能获得最佳体验</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">📝</div>
						<h3 class="feature-title">技术分享</h3>
						<p class="feature-description">分享前端开发、Web 技术和编程心得</p>
					</div>
				</div>

				<div class="blog-showcase">
					<div class="showcase-content">
						<div class="showcase-text">
							<h2 class="showcase-title">最新文章</h2>
							<p class="showcase-description">
								探索技术世界，记录学习历程，分享开发经验。
								从基础到进阶，从理论到实践，一起成长。
							</p>
							<div class="showcase-stats">
								<div class="stat-item">
									<div class="stat-number">5+</div>
									<div class="stat-label">文章</div>
								</div>
								<div class="stat-item">
									<div class="stat-number">3</div>
									<div class="stat-label">分类</div>
								</div>
							</div>
						</div>
						<div class="showcase-visual">
							<div class="blog-preview">
								<div class="preview-card">
									<h4 class="preview-card-title">欢迎使用 Astro</h4>
									<p class="preview-card-meta">2024年1月9日 · 技术分享</p>
								</div>
								<div class="preview-card">
									<h4 class="preview-card-title">Markdown 样式指南</h4>
									<p class="preview-card-meta">2024年1月9日 · 教程</p>
								</div>
								<div class="preview-card">
									<h4 class="preview-card-title">第二篇文章</h4>
									<p class="preview-card-meta">2024年1月9日 · 随笔</p>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<section class="cta-section">
				<h2 class="cta-title">开始阅读</h2>
				<p class="cta-description">探索我的文章，发现更多有趣的技术内容和见解</p>
				<a href="/blog" class="cta-button">
					浏览所有文章
					<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M5 12h14M12 5l7 7-7 7"/>
					</svg>
				</a>
			</section>
		</div>

		<BackToTop />
		<Footer />
	</body>
</html>
