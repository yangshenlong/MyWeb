---
import BaseHead from '../components/BaseHead.astro';
import HeaderArchitectural from '../components/HeaderArchitectural.astro';
import GlassCard from '../components/GlassCard.astro';
import BackToTop from '../components/BackToTop.astro';
import ScrollProgress from '../components/ScrollProgress.astro';
import SkipLink from '../components/SkipLink.astro';
import { SITE_TITLE, SITE_DESCRIPTION, AUTHOR } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`About | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
	</head>

	<body>
		<ScrollProgress />
		<SkipLink />
		<HeaderArchitectural />

		<!-- Page Header -->
		<section class="page-header-section">
			<div class="header-container">
				<div class="header-content">
					<div class="header-top">
						<div class="header-label">
							<span class="label-line"></span>
							<span class="label-text">关于</span>
						</div>
						<h1 class="page-title">{AUTHOR.name}</h1>
					</div>
					<div class="header-bottom">
						<p class="page-subtitle">技术美术</p>
						<p class="page-description">{AUTHOR.bio}</p>
						<div class="contact-links">
						<a href="mailto:xgg000303@163.com" class="contact-link" data-copy="xgg000303@163.com">
							<span class="contact-label">邮箱</span>
							<span class="contact-value">xgg000303@163.com</span>
						</a>
						<div class="contact-link" data-copy="2427900391">
							<span class="contact-label">QQ</span>
							<span class="contact-value">2427900391</span>
						</div>
						<div class="contact-link" data-copy="18365912171">
							<span class="contact-label">微信</span>
							<span class="contact-value">18365912171</span>
						</div>
						<div class="contact-link" data-copy="+86 18365912171">
							<span class="contact-label">手机</span>
							<span class="contact-value">+86 18365912171</span>
						</div>
					</div>
					<div class="copy-toast" id="copyToast">已复制</div>
				</div>
				</div>
				<div class="header-photo">
					<img src="/images/profile-photo.jpg" alt="个人照片" />
				</div>
			</div>
			<div class="header-decoration">
				<span class="deco-number">04</span>
			</div>
		</section>

		<!-- Skills Section -->
		<section class="skills-section">
			<div class="skills-container">
				<div class="section-header">
					<div class="header-label">
						<span class="label-line"></span>
						<span class="label-text">专业技能</span>
					</div>
					<h2 class="section-title">技能与技术</h2>
				</div>

				<div class="skills-grid">
					<GlassCard class="skill-card">
						<div class="skill-header">
							<span class="skill-icon">▣</span>
							<h3 class="skill-title">Shader Development</h3>
						</div>
						<ul class="skill-list">
							<li>HLSL / GLSL</li>
							<li>Unity URP / HDRP</li>
							<li>Shader Graph</li>
							<li>Compute Shader</li>
							<li>Post Processing</li>
						</ul>
					</GlassCard>

					<GlassCard class="skill-card">
						<div class="skill-header">
							<span class="skill-icon">⚡</span>
							<h3 class="skill-title">Rendering</h3>
						</div>
						<ul class="skill-list">
							<li>PBR Rendering</li>
							<li>Deferred / Forward</li>
							<li>Real-time GI</li>
							<li>Lighting Systems</li>
							<li>Performance Opt</li>
						</ul>
					</GlassCard>

					<GlassCard class="skill-card">
						<div class="skill-header">
							<span class="skill-icon">🔧</span>
							<h3 class="skill-title">Tools & Pipeline</h3>
						</div>
						<ul class="skill-list">
							<li>Python / PyQt</li>
							<li>C# / .NET</li>
							<li>Maya / Houdini</li>
							<li>Blender API</li>
							<li>Workflow Design</li>
						</ul>
					</GlassCard>

					<GlassCard class="skill-card">
						<div class="skill-header">
							<span class="skill-icon">🎨</span>
							<h3 class="skill-title">Creative</h3>
						</div>
						<ul class="skill-list">
							<li>Visual Design</li>
							<li>VFX Creation</li>
							<li>Art Direction</li>
							<li>3D Modeling</li>
							<li>Texturing</li>
						</ul>
					</GlassCard>
				</div>
			</div>
		</section>

		<!-- Footer -->
		<footer class="arch-footer">
			<div class="footer-content">
				<div class="footer-left">
					<span class="footer-text">© 2024 杨慎龙</span>
					<span class="footer-divider">·</span>
					<span class="footer-text">Technical Artist</span>
				</div>
				<div class="footer-right">
					<a href="https://github.com/yangshenlong" target="_blank" class="footer-link">GitHub</a>
					<a href="#" target="_blank" class="footer-link">Bilibili</a>
					<a href="#" target="_blank" class="footer-link">知乎</a>
				</div>
			</div>
		</footer>

		<BackToTop />

		<script>
			// 点击复制功能
			const contactLinks = document.querySelectorAll('.contact-link');
			const toast = document.getElementById('copyToast');

			contactLinks.forEach(link => {
				link.addEventListener('click', async (e) => {
					// 对于邮箱链接，只在用户按住 Ctrl/Cmd 时才打开邮件客户端
					if (link.tagName === 'A' && !e.ctrlKey && !e.metaKey) {
						e.preventDefault();
					} else if (link.tagName === 'A' && (e.ctrlKey || e.metaKey)) {
						return; // 允许默认的邮件客户端打开
					}

					const textToCopy = link.getAttribute('data-copy');
					if (textToCopy) {
						try {
							await navigator.clipboard.writeText(textToCopy);
							// 显示复制提示
							toast.classList.add('show');
							setTimeout(() => {
								toast.classList.remove('show');
							}, 1500);
						} catch (err) {
							console.error('复制失败:', err);
						}
					}
				});
			});
		</script>

		<style>
			.page-header-section {
				padding: calc(60px + var(--section-padding-y, 8em)) 4em 6em;
				background: var(--color-bg-primary);
				position: relative;
			}

			.header-container {
				max-width: 1200px;
				margin: 0 auto;
				display: grid;
				grid-template-columns: 1fr auto;
				gap: 4em;
				align-items: center;
			}

			.header-top {
				display: flex;
				flex-direction: column;
				gap: 0.5em;
			}

			.header-label {
				display: flex;
				align-items: center;
				gap: 1em;
			}

			.label-line {
				width: 30px;
				height: 1px;
				background: rgba(255, 255, 255, 0.2);
			}

			.label-text {
				font-size: 0.7em;
				letter-spacing: 0.3em;
				text-transform: uppercase;
				color: rgba(255, 255, 255, 0.4);
				font-family: 'Space Mono', monospace;
			}

			.page-title {
				font-size: clamp(2.5em, 6vw, 4em);
				font-weight: 200;
				letter-spacing: -0.02em;
				line-height: 1.1;
				margin: 0;
			}

			.header-bottom {
				display: flex;
				flex-direction: column;
				gap: 0.5em;
			}

			.header-content {
				display: flex;
				flex-direction: column;
				gap: 0.5em;
			}

			.header-photo {
				width: 280px;
				height: 280px;
				border-radius: 50%;
				overflow: hidden;
				border: 3px solid rgba(255, 255, 255, 0.1);
				box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
				flex-shrink: 0;
			}

			.header-photo img {
				width: 100%;
				height: 100%;
				object-fit: cover;
			}

			.page-subtitle {
				font-size: 1.1em;
				letter-spacing: 0.2em;
				text-transform: uppercase;
				color: rgba(255, 255, 255, 0.6);
				margin: 0;
			}

			.page-description {
				font-size: 1em;
				line-height: 1.7;
				color: var(--color-text-tertiary);
				max-width: 500px;
			}

			.contact-links {
				display: flex;
				flex-direction: column;
				gap: 0.75em;
				margin-top: 1.5em;
			}

			.contact-link {
				display: flex;
				align-items: center;
				gap: 1em;
				transition: all 0.3s ease;
			}

			.contact-link:hover {
				transform: translateX(5px);
			}

			.contact-label {
				font-size: 0.75em;
				letter-spacing: 0.15em;
				text-transform: uppercase;
				color: rgba(255, 255, 255, 0.4);
				font-family: 'Space Mono', monospace;
				min-width: 3em;
			}

			.contact-value {
				font-size: 0.95em;
				color: rgba(255, 255, 255, 0.8);
				font-family: 'Space Mono', monospace;
				transition: color 0.3s ease;
			}

			.contact-link:hover .contact-value {
				color: rgba(255, 255, 255, 1);
			}

			.contact-link {
				cursor: pointer;
			}

			.copy-toast {
				position: fixed;
				top: 50%;
				left: 50%;
				transform: translate(-50%, -50%);
				padding: 1em 2em;
				background: var(--color-text-primary);
				color: var(--color-bg-primary);
				font-size: 0.9em;
				font-weight: 500;
				letter-spacing: 0.1em;
				border-radius: 2px;
				opacity: 0;
				pointer-events: none;
				transition: all 0.3s ease;
				z-index: 1000;
			}

			.copy-toast.show {
				opacity: 1;
			}

			.header-decoration {
				position: relative;
			}

			.deco-number {
				font-size: 6em;
				font-weight: 100;
				color: rgba(255, 255, 255, 0.03);
				font-family: 'Space Mono', monospace;
				line-height: 1;
			}

			/* Skills Section */
			.skills-section {
				padding: 6em 4em;
				background: var(--color-bg-primary);
			}

			.skills-container {
				max-width: 1200px;
				margin: 0 auto;
			}

			.section-header {
				margin-bottom: 3em;
			}

			.section-title {
				font-size: clamp(2em, 4vw, 3em);
				font-weight: 200;
				letter-spacing: -0.02em;
				color: rgba(255, 255, 255, 0.9);
				margin: 0.5em 0 0 0;
			}

			.skills-grid {
				display: grid;
				grid-template-columns: repeat(4, 1fr);
				gap: 1.5em;
			}

			.skill-card {
				min-height: 250px;
			}

			.skill-header {
				display: flex;
				align-items: center;
				gap: 0.75em;
				margin-bottom: 1.5em;
			}

			.skill-icon {
				font-size: 1.5em;
			}

			.skill-title {
				font-size: 1.1em;
				font-weight: 400;
				color: var(--color-text-primary);
				margin: 0;
			}

			.skill-list {
				list-style: none;
				padding: 0;
				margin: 0;
				display: flex;
				flex-direction: column;
				gap: 0.5em;
			}

			.skill-list li {
				font-size: 0.9em;
				color: rgba(255, 255, 255, 0.6);
				padding-left: 1em;
				position: relative;
			}

			.skill-list li::before {
				content: '—';
				position: absolute;
				left: 0;
				color: rgba(255, 255, 255, 0.2);
			}

			/* Footer styles moved to Footer.astro component */

			/* Responsive */
			@media (max-width: 1024px) {
				.page-header-section,
				.skills-section {
					padding-left: 2em;
					padding-right: 2em;
				}

				.header-container {
					grid-template-columns: 1fr;
					gap: 2em;
				}

				.header-content {
					align-items: center;
					text-align: center;
				}

				.header-photo {
					margin: 0 auto;
				}

				.header-decoration {
					align-self: flex-end;
				}

				.deco-number {
					font-size: 4em;
				}

				.skills-grid {
					grid-template-columns: repeat(2, 1fr);
				}
			}

			@media (max-width: 768px) {
				.skills-grid {
					grid-template-columns: 1fr;
				}

				.footer-content {
					flex-direction: column;
					gap: 1.5em;
					text-align: center;
				}

				.footer-left {
					flex-direction: column;
					gap: 0.5em;
				}

				.footer-right {
					gap: 1.5em;
				}
			}
		</style>
	</body>
</html>
