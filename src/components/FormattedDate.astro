---
import { formatDate } from '../utils/format';

interface Props {
	date: Date;
	format?: 'full' | 'long' | 'medium' | 'short' | 'relative';
}

const { date, format = 'long' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{formatDate(date, 'zh-CN', format)}
</time>
